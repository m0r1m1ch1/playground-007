<canvas class="canvas" data-overlay-canvas></canvas>

<style lang="scss">
  @use '@/styles/extension' as *;

  .canvas {
    position: fixed;
    inset: 0;
    z-index: calc(infinity * 1);
    width: 100%;
    height: 100%;
    pointer-events: none;
  }
</style>
